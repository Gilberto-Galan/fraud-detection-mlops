services:
  fraud_api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models

  mlflow_ui:
    image: ghcr.io/mlflow/mlflow
    ports:
      - "5000:5000"
    command: mlflow ui --host 0.0.0.0